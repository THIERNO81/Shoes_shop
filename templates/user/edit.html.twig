


{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Edit User</h1>

    {# Inclure le formulaire de modification avec le label du bouton "Update" #}
    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    {# Lien de retour vers la page d'édition de l'utilisateur #}
    <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn primary">Retour</a>

    {# Inclure le formulaire de suppression de l'utilisateur #}
    {{ include('user/_delete_form.html.twig') }}
{% endblock %}

