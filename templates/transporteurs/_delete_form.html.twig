<form method="post" action="{{ path('app_transporteurs_delete', {'id': transporteur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transporteur.id) }}">
    <button class="" class="btn btn-danger btn-sm">Delete</button>
</form>
